import React from "react";
import "../styles/global.css"

export default function Dashboard() {
  return (
    <div className="page">
      <div className="pageCard">
        <h2>Dashboard</h2>
        <p className="muted">Empty for now. Next we’ll build widgets & summary cards.</p>
      </div>
    </div>
  );
}